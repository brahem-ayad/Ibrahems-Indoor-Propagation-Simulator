#pragma once

#include<raylib.h>

namespace conf {
  extern int screen_width;
  extern int screen_height;

  extern float grid_size; // the width/height of each square in the grid, in px.
  extern float grid_lines_width;
  extern Color grid_color;
}
